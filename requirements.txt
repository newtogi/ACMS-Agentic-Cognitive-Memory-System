# Agent Brain — runtime dependencies
# L1/L6 talk to Ollama over the HTTP API using only the Python standard library.
# L7 (memory graph / dream cycle) requires the two packages below.

networkx>=3.0,<4
requests>=2.31,<3

# Optional (unused by default):
# aiohttp>=3.9.0
# pydantic>=2.0.0
